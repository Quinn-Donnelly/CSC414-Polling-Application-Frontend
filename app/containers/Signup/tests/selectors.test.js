// import { fromJS } from 'immutable';
// import { makeSelectSignupDomain } from '../selectors';

// const selector = makeSelectSignupDomain();

describe('makeSelectSignupDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
