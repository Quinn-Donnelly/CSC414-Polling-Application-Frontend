// import { fromJS } from 'immutable';
// import { makeSelectCoursePageDomain } from '../selectors';

// const selector = makeSelectCoursePageDomain();

describe('makeSelectCoursePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
