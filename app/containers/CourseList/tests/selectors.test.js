// import { fromJS } from 'immutable';
// import { makeSelectCourseListDomain } from '../selectors';

// const selector = makeSelectCourseListDomain();

describe('makeSelectCourseListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
