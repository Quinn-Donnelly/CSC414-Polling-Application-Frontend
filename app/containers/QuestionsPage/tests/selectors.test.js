// import { fromJS } from 'immutable';
// import { makeSelectQuestionsPageDomain } from '../selectors';

// const selector = makeSelectQuestionsPageDomain();

describe('makeSelectQuestionsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
